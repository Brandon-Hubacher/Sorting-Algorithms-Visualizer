import javax.swing.*;
import java.awt.*;

public class Main {
    /*
    public static void main(String[] args)
    {

        JFrame frame = new JFrame();
        frame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        //GenerateBars bars = new GenerateBars(100);
        //frame.add(bars);
        //JPanel topPanel = new JPanel(new BorderLayout());
        SortingAlgorithms sort = new SortingAlgorithms(100);
        //sort.bars.setBounds(0, 0, 1280, 800);
        frame.add(sort.bars);
        frame.setSize(1280, 720);
        frame.setResizable(true);
        frame.setVisible(true);

        //sort.mergeSort(SortingAlgorithms.arr, new int[SortingAlgorithms.arr.length], 0, SortingAlgorithms.arr.length - 1);
        //sort.insertionSort();
        /*
        // a = Point(1, 1)
        Point a = new Point(1, 1);
        // (1, 1)
        Point b = new Point(3, 5);
        System.out.println(a.toString());


    }

     */

}
